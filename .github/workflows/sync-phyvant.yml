name: Sync Phyvant Documentation

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout docs repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Mintlify CLI
        run: npm i -g mint

      - name: Checkout Phyvant repository
        run: |
          git clone https://x-access-token:${{ secrets.PHYVANT_PAT }}@github.com/nihalgunu/phyvant.git phyvant-temp
          cd phyvant-temp
          git checkout main

      - name: Copy files from Phyvant repository
        run: |
          mkdir -p openapi content/sdk
          cp phyvant-temp/backend/docs/openapi/openapi.yaml openapi/openapi.yaml
          cp phyvant-temp/sdk/python/README.md content/sdk/README.md

      - name: Validate OpenAPI spec
        run: mint openapi-check openapi/openapi.yaml

      - name: Check for broken links
        run: mint broken-links

      - name: Check for changes
        id: check-changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "changes=true" >> $GITHUB_OUTPUT
          else
            echo "changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Configure Git
        if: steps.check-changes.outputs.changes == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

      - name: Commit and push changes
        if: steps.check-changes.outputs.changes == 'true'
        run: |
          git add -A
          git commit -m "chore: sync documentation from phyvant repo"
          git push

